import { FabProps as MuiFabProps } from '@mui/material/Fab';
import { BaseComponentProps } from '../../base/base-component-props';

export interface WFloatingActionButtonProps extends BaseComponentProps, MuiFabProps {
  // WFace specific props can be added here later
  // e.g., tooltip?: string;
}
